### Curated JSON lists are saved here

Curated debloat lists, e.g. derived from the docs in `debloat/`. Debloat lets
you pick lists from this directory or from the generated `lists/exported/`.
Entries carry `id`, `list`, `description`, and `status`; the `removal` field is
optional.

The generated lists are git-ignored; only this README is tracked.